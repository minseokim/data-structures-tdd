/* Describe block is used to group individual tests, the first group tests binary search trees */
describe("Binary Search Tree", () => {
  let BST;

  beforeEach(function() {
    BST = new BinarySearchTree();
  });
  // 'it' is used to create actual unit tests, with the first parameter giving a readable description of the test
  it("should have a BinarySearchTree class", () => {});
});
